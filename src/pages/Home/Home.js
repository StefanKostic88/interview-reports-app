import { MainContainer, SearchHeader, CardsContainer } from "../../components";

const Home = () => {
  return (
    <MainContainer>
      <SearchHeader />
      <CardsContainer />
    </MainContainer>
  );
};

export default Home;
